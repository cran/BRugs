model{

		halfPi <- 3.141592659 / 2
		int <- integral(F(x), 0, halfPi, 1.0E-6)
		F(x) <- cos(x)
	
		sol <- solution(F(y), 0, 1, 1.0E-6)
		F(y) <- y * y - pow(1 - y, 5)
		zero <- sol * sol - pow(1 - sol, 5)
	}